def twoSum(self, nums: List[int], target: int) -> List[int]:
        for ele in range(len(nums)):
            for j in range (ele+1, len(nums)):
                if nums[ele] + nums[j] == target:
                    return ele, j
